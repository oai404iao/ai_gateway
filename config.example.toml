# Copy this file to config.toml when creating an environment-specific setup.
# Do not use a .env file; the application loads TOML configuration directly.

[server]
host = "127.0.0.1"
port = 3000
max_request_body_bytes = 1_048_576

[database]
url = "postgres://ai_gateway:ai_gateway@127.0.0.1:5432/ai_gateway"
max_connections = 10
connect_timeout_seconds = 5

[upstream]
connect_timeout_seconds = 10
response_header_timeout_seconds = 30
stream_idle_timeout_seconds = 90

[runtime_config]
reload_interval_seconds = 30

[observability]
filter = "ai_gateway=debug,tower_http=info"

# The following dynamic routing records are compiled into an immutable runtime
# snapshot at startup. Client key plaintext is discarded after compilation; use
# a unique production key in a non-versioned config.local.toml.
#
# [[api_keys]]
# id = "local-client"
# key = "replace-with-a-long-random-client-key"
# allowed_api_formats = ["open_ai_chat_completions", "open_ai_responses"]
# permissions = ["proxy", "models.read"]
#
# [[channels]]
# id = "openai-chat"
# api_format = "open_ai_chat_completions"
# base_url = "https://api.openai.com"
# upstream_bearer_token = "replace-with-upstream-key"
#
# [[channels]]
# id = "openai-responses"
# api_format = "open_ai_responses"
# base_url = "https://api.openai.com"
# upstream_bearer_token = "replace-with-upstream-key"
#
# [[model_rules]]
# client_model = "gpt-4.1"
# api_format = "open_ai_chat_completions"
# upstream_model = "gpt-4.1-2025-04-14"
# channel_id = "openai-chat"
#
# [[model_rules]]
# client_model = "gpt-4.1"
# api_format = "open_ai_responses"
# upstream_model = "gpt-4.1-2025-04-14"
# channel_id = "openai-responses"
