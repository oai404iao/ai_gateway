# MVP 开发进度

本文件是第一期的持久化任务清单。每个阶段开始前先明确目标和任务；阶段内所有任务及验证通过后才创建提交并进入下一阶段。

## 阶段 1：编译配置快照与领域路由类型

目标：将 TOML 中的客户端 Key、模型规则和单渠道配置编译为 `ArcSwap` 持有的只读快照；快照不保留明文客户端 Key，并严格隔离两个 API 格式。

- [x] 定义 `ApiKeyHash`、权限、编译后的 API Key、模型规则和渠道领域类型。
- [x] 扩展 TOML 配置，定义 API Key、模型规则和渠道输入配置。
- [x] 实现配置编译、格式/引用/唯一性校验，以及 `ArcSwap<CompiledRuntimeConfig>`。
- [x] 为哈希鉴权、规则匹配、格式隔离、配置校验和原子替换编写单元测试。
- [x] 更新示例配置与一期使用说明。
- [x] 运行格式化、lint 和测试，并提交阶段成果。

## 阶段 2：认证、模型列表和透明代理

目标：提供受 Bearer Key 保护的 `/v1/models`、`/v1/chat/completions` 与 `/v1/responses`，在选定单渠道后直通或仅替换模型别名。

状态：已完成。

- [x] 建立带运行时快照和复用 reqwest 客户端的应用状态。
- [x] 实现 OpenAI 风格错误、Bearer 鉴权和按格式的权限检查。
- [x] 实现按 API Key 可见的 `/v1/models`。
- [x] 实现两个格式隔离的代理入口、规则匹配、单渠道选择及安全的头转发。
- [x] 实现原始请求字节直通和仅修改顶层 `model` 的别名替换。
- [x] 为未授权、未知模型、格式不匹配、非流式代理、模型列表和原始字节直通编写集成测试。
- [x] 运行格式化、lint 和测试，并提交阶段成果。

## 阶段 3：流式响应、日志与断连取消

目标：不缓冲上游响应，记录渠道、延迟和结果，并确保下游断连会释放上游流。

状态：已完成。

- [x] 实现响应头/流空闲超时和上游响应 hop-by-hop 头清理。
- [x] 将 reqwest 字节流直接交给 Axum 响应体，不创建后台转发任务。
- [x] 实现请求完成、失败和取消的可追踪日志事件（渠道、状态、延迟、结果）。
- [x] 用真实 TCP 上游验证 SSE 逐块直通、无缓冲和客户端断连取消上游。
- [x] 运行格式化、lint 和测试，并提交阶段成果。

## 阶段 4：交付收尾

目标：验证一期行为、完善操作说明，并确保没有把后续功能提前引入请求链路。

状态：已完成。

- [x] 复核两个格式绝不互相回退，且任一请求只选择一个渠道、不重试。
- [x] 复核示例配置、启动说明和已实现范围；明确权重、健康检查、变换、同步和计费仍在后续阶段。
- [x] 运行完整 `cargo fmt --check`、`cargo clippy --all-targets` 和 `cargo test`。
- [x] 更新本文件为完成状态并提交最终收尾。
