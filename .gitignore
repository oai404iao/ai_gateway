# Rust build artifacts
/target/
*.rs.bk

# SQLx's offline query cache can be regenerated locally
/.sqlx/

# Local tooling and editor state
.idea/
.vscode/
*.swp
*.swo
*~

# Runtime logs and current-working-directory local configuration
*.log
/config.toml
/config.local.toml
/console-jwt-private.pem
/console-jwt-public.pem
/data/request-log-spool/

# `.env.real-upstream` is permitted only for the opt-in real-upstream smoke
# script. It is always local; the tracked template contains no credentials.
.env
.env.*
!.env.example
!.env.real-upstream.example
